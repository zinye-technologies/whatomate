---
title: API Overview
description: Introduction to the Whatomate REST API
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Base URL

All API requests should be made to:

```
http://your-server:8080/api
```

## Authentication

The Whatomate API uses JWT (JSON Web Tokens) for authentication. Include the access token in the Authorization header:

```bash
Authorization: Bearer <access_token>
```

See [Authentication](/whatomate/api-reference/authentication) for details on obtaining tokens.

## Response Format

All responses are returned in JSON format with the following structure:

### Success Response

```json
{
  "status": "success",
  "data": {
    // Response data
  }
}
```

### Error Response

```json
{
  "status": "error",
  "message": "Error description",
  "data": null
}
```

## HTTP Status Codes

| Code | Description |
|------|-------------|
| 200 | Success |
| 201 | Created |
| 400 | Bad Request - Invalid parameters |
| 401 | Unauthorized - Invalid or missing token |
| 403 | Forbidden - Insufficient permissions |
| 404 | Not Found |
| 500 | Internal Server Error |

## Rate Limiting

API requests are subject to rate limiting to ensure fair usage:

- **Standard**: 100 requests per minute
- **Bulk operations**: 10 requests per minute

Rate limit headers are included in responses:

```
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 95
X-RateLimit-Reset: 1640000000
```

## Pagination

List endpoints support pagination using `page` and `limit` query parameters:

```bash
GET /api/contacts?page=1&limit=20
```

Paginated responses include:

```json
{
  "status": "success",
  "data": {
    "items": [...],
    "total": 100,
    "page": 1,
    "limit": 20,
    "total_pages": 5
  }
}
```

## API Endpoints

<CardGrid>
  <Card title="Authentication" icon="approve-check">
    Register, login, and manage tokens. [View →](/whatomate/api-reference/authentication)
  </Card>
  <Card title="Users" icon="setting">
    User management (Admin only). [View →](/whatomate/api-reference/users)
  </Card>
  <Card title="Organizations" icon="star">
    Organization and member management. [View →](/whatomate/api-reference/organizations)
  </Card>
  <Card title="Contacts" icon="open-book">
    Manage contacts and assignments. [View →](/whatomate/api-reference/contacts)
  </Card>
  <Card title="Messages" icon="comment">
    Send and receive messages. [View →](/whatomate/api-reference/messages)
  </Card>
  <Card title="Templates" icon="document">
    Manage message templates. [View →](/whatomate/api-reference/templates)
  </Card>
  <Card title="Flows" icon="puzzle">
    WhatsApp Flows management. [View →](/whatomate/api-reference/flows)
  </Card>
  <Card title="Chatbot" icon="setting">
    Chatbot configuration. [View →](/whatomate/api-reference/chatbot)
  </Card>
  <Card title="Webhooks" icon="rocket">
    Receive real-time updates. [View →](/whatomate/api-reference/webhooks)
  </Card>
  <Card title="Analytics" icon="graph">
    Dashboard and message analytics. [View →](/whatomate/api-reference/analytics)
  </Card>
</CardGrid>

## Usage meter

Approximate outbound send counts by WhatsApp category (service / utility / marketing / authentication), plus the 1k free service allowance remaining per phone:

See [Usage & billing meter](/whatomate/api-reference/usage).

